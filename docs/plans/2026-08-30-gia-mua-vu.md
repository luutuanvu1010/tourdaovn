# Giá mùa vụ — kế hoạch thi công

> **Cho tác nhân thi hành:** BẮT BUỘC dùng `superpowers:subagent-driven-development` (khuyến
> nghị) hoặc `superpowers:executing-plans` để chạy từng task. Các bước dùng ô tick `- [ ]`.

**Mục tiêu:** giá tour tự cộng hoặc trừ theo mùa mà biên tập khai trong Sanity Studio, tính
theo ngày khởi hành khách chọn, không phá ràng buộc "giá chỉ đọc lúc dựng trang".

**Kiến trúc:** mùa là một danh sách **có thứ tự** trong Studio (`bangGiaMuaVu`, tài liệu đơn).
Lúc dựng trang, danh sách mùa áp được cho từng tour được nướng vào form cùng bảng giá gốc.
Trình duyệt chọn mùa đầu tiên phủ ngày khởi hành rồi áp phần trăm — không lời gọi mạng nào.
Máy chủ vẫn chỉ kiểm nhất quán số học, không tin giá.

**Công nghệ:** Astro 5 · Sanity Studio v3 (`cms/`) · TypeScript · vitest (`test/booking/`) ·
Cloudflare D1.

**Đặc tả:** `docs/adr/ADR-0030-ba-lop-nghiep-vu-be-mat-quan-tri.md` §3 (đã phê chuẩn
2026-08-30).

## Global Constraints

Mọi task đều phải giữ, không có ngoại lệ:

- **Trang tính Google Sheet là nguồn giá duy nhất.** Studio **không** giữ con số tiền nào — chỉ
  giữ quy tắc: khung thời gian, phần trăm, áp cho tour nào. Xoá sạch dữ liệu mùa thì giá gốc
  trong `data/prices.yaml` vẫn nguyên vẹn.
- **`BK1`:** endpoint `/api/dat-tour` và `src/lib/booking/*` **không** được import
  `lib/prices.ts`, `lib/sanity.ts`, `lib/resolver.ts`. Mùa đi vào form qua `TourDetail.astro`
  lúc dựng, không qua lời gọi lúc chạy.
- **`BK5`:** một hàm tính giá dùng chung cho cả trình duyệt lẫn máy chủ. Không viết hai bản.
- **Ưu tiên theo thứ tự, cái trên thắng cái dưới.** Duyệt danh sách từ trên xuống, gặp mùa đầu
  tiên phủ ngày khởi hành và áp được cho tour đó thì dùng luôn rồi **dừng**. Không so số học,
  không cộng dồn. Hai mùa phủ nhau là **hợp lệ**, không phải lỗi.
- **Làm tròn LÊN nghìn** sau khi áp phần trăm: `Math.ceil(x / 1000) * 1000`.
- **Mùa tính theo `departDate`** (ngày khởi hành), không phải ngày đặt.
- **Phần trăm dương là tăng, âm là giảm.** Hạng có giá 0 (em bé miễn phí) nhân bao nhiêu vẫn 0.
- **Không thêm phụ thuộc lúc chạy** (`ADR-0027` quyết định 7).
- Cổng phải xanh trước mỗi commit: `npx astro check` 0 errors · `npx vitest run` toàn bộ pass.

---

### Task 1: Kiểu dữ liệu mùa và hàm chọn mùa

Lớp nghiệp vụ thuần — không biết Sanity, không biết web. Đây là nơi luật "trên thắng dưới" sống.

**Files:**
- Create: `src/lib/booking/season.ts`
- Test: `test/booking/season.test.ts`

**Interfaces:**
- Consumes: không có (task đầu).
- Produces: `type Season = { name: string; from: string; to: string; percent: number }`;
  `function pickSeason(seasons: Season[], departDate: string): Season | null`.
  `from`/`to`/`departDate` đều là chuỗi `YYYY-MM-DD`. Việc lọc theo tour (áp cho / trừ ra) đã
  làm xong lúc dựng trang, nên hàm này **không** nhận tham số tour.

- [ ] **Bước 1: Viết test thất bại**

```ts
// test/booking/season.test.ts
import { describe, expect, it } from 'vitest'
import { pickSeason, type Season } from '../../src/lib/booking/season'

const TET: Season = { name: 'Tết', from: '2027-02-05', to: '2027-02-15', percent: 40 }
const LE_304: Season = { name: 'Lễ 30/4', from: '2027-04-30', to: '2027-05-01', percent: 30 }
const HE: Season = { name: 'Cao điểm hè', from: '2027-06-01', to: '2027-08-31', percent: 20 }
const THAP_DIEM: Season = { name: 'Thấp điểm', from: '2027-09-15', to: '2027-11-30', percent: -15 }
const DS = [TET, LE_304, HE, THAP_DIEM]

describe('pickSeason', () => {
  it('không mùa nào phủ → null', () => {
    expect(pickSeason(DS, '2027-03-10')).toBeNull()
  })
  it('chỉ một mùa phủ → mùa đó', () => {
    expect(pickSeason(DS, '2027-07-01')?.name).toBe('Cao điểm hè')
  })
  it('ngày đầu và ngày cuối của khoảng đều tính là phủ', () => {
    expect(pickSeason(DS, '2027-06-01')?.name).toBe('Cao điểm hè')
    expect(pickSeason(DS, '2027-08-31')?.name).toBe('Cao điểm hè')
  })
  it('CÁI TRÊN THẮNG: 30/4 nằm trong hè nhưng lễ đứng trên', () => {
    const ds: Season[] = [LE_304, { ...HE, from: '2027-04-01' }]
    expect(pickSeason(ds, '2027-04-30')?.name).toBe('Lễ 30/4')
  })
  it('đảo thứ tự thì kết quả đảo theo — không so số học', () => {
    const ds: Season[] = [{ ...HE, from: '2027-04-01' }, LE_304]
    expect(pickSeason(ds, '2027-04-30')?.name).toBe('Cao điểm hè')
  })
  it('mùa giảm giá cũng chọn như mọi mùa khác', () => {
    expect(pickSeason(DS, '2027-10-01')?.percent).toBe(-15)
  })
  it('mùa một ngày (ngày lễ rời rạc)', () => {
    const ds: Season[] = [{ name: 'Quốc khánh', from: '2027-09-02', to: '2027-09-02', percent: 25 }]
    expect(pickSeason(ds, '2027-09-02')?.name).toBe('Quốc khánh')
    expect(pickSeason(ds, '2027-09-03')).toBeNull()
  })
  it('danh sách rỗng → null', () => {
    expect(pickSeason([], '2027-07-01')).toBeNull()
  })
  it('mùa khai ngược đầu đuôi thì bỏ qua, không ném', () => {
    const ds: Season[] = [{ name: 'Sai', from: '2027-08-31', to: '2027-06-01', percent: 50 }, HE]
    expect(pickSeason(ds, '2027-07-01')?.name).toBe('Cao điểm hè')
  })
})
```

- [ ] **Bước 2: Chạy test cho thất bại**

Chạy: `npx vitest run test/booking/season.test.ts`
Kỳ vọng: FAIL — không tìm thấy module `season`.

- [ ] **Bước 3: Viết mã tối thiểu**

```ts
// src/lib/booking/season.ts
// Lớp nghiệp vụ thuần: không import Sanity, không import prices (BK1). So chuỗi ngày dạng
// YYYY-MM-DD trực tiếp — chuỗi ISO ngày so từ điển đúng bằng so thời gian, nên không cần
// dựng Date và không dính bẫy múi giờ.

export type Season = {
  name: string
  /** YYYY-MM-DD, tính cả ngày này */
  from: string
  /** YYYY-MM-DD, tính cả ngày này */
  to: string
  /** dương là tăng, âm là giảm */
  percent: number
}

const ISO = /^\d{4}-\d{2}-\d{2}$/

/**
 * Mùa đầu tiên trong danh sách phủ `departDate`. Thứ tự danh sách LÀ độ ưu tiên: cái trên
 * thắng cái dưới (ADR-0030 §3). Không so số học — hai mùa phủ nhau là hợp lệ, biên tập sắp
 * thứ tự trong Studio để quyết định cái nào thắng.
 */
export function pickSeason(seasons: Season[], departDate: string): Season | null {
  if (!ISO.test(departDate)) return null
  for (const s of seasons) {
    if (!ISO.test(s.from) || !ISO.test(s.to)) continue
    // Khai ngược đầu đuôi thì bỏ qua thay vì ném: dữ liệu đến từ biên tập, một dòng hỏng
    // không được làm chết cả form. Validator lúc dựng mới là nơi báo đỏ.
    if (s.from > s.to) continue
    if (departDate >= s.from && departDate <= s.to) return s
  }
  return null
}
```

- [ ] **Bước 4: Chạy test cho pass**

Chạy: `npx vitest run test/booking/season.test.ts`
Kỳ vọng: PASS 9/9.

- [ ] **Bước 5: Commit**

```bash
git add src/lib/booking/season.ts test/booking/season.test.ts
git commit -m "feat(gia): kieu Season va ham chon mua theo thu tu uu tien"
```

---

### Task 2: Áp mùa vào phép tính giá

**Files:**
- Modify: `src/lib/booking/quote.ts`
- Test: `test/booking/quote.test.ts` (thêm khối mới, giữ nguyên 11 ca đang có)

**Interfaces:**
- Consumes: `Season`, `pickSeason` từ Task 1.
- Produces: `computeQuote(table, pax, opts?)` với
  `opts?: { seasons?: Season[]; departDate?: string }`; `Quote` thêm trường
  `season?: { name: string; percent: number }`. Không truyền `opts` thì hành vi **y hệt hiện
  nay** — mọi lời gọi cũ vẫn đúng.

- [ ] **Bước 1: Viết test thất bại**

```ts
// thêm vào cuối test/booking/quote.test.ts
import { pickSeason, type Season } from '../../src/lib/booking/season'

describe('computeQuote với mùa', () => {
  const bang: PriceTable = { kind: 'flat', perPax: { adult: 740000, child: 430000, infant: 0 } }
  const mua: Season[] = [
    { name: 'Lễ 30/4', from: '2027-04-30', to: '2027-05-01', percent: 30 },
    { name: 'Cao điểm hè', from: '2027-06-01', to: '2027-08-31', percent: 20 },
    { name: 'Thấp điểm', from: '2027-10-01', to: '2027-11-30', percent: -15 },
  ]

  it('không mùa nào phủ → giá gốc, không có trường season', () => {
    const q = computeQuote(bang, { adult: 2, child: 0, senior: 0, infant: 0 }, { seasons: mua, departDate: '2027-03-10' })
    expect(q?.total).toBe(1480000)
    expect(q?.season).toBeUndefined()
  })

  it('mùa tăng 30%: từng hạng nhân rồi làm tròn LÊN nghìn', () => {
    const q = computeQuote(bang, { adult: 2, child: 1, senior: 0, infant: 1 }, { seasons: mua, departDate: '2027-04-30' })
    // 740.000 × 1,3 = 962.000 ; 430.000 × 1,3 = 559.000 ; em bé 0 vẫn 0
    expect(q?.perPax).toEqual({ adult: 962000, child: 559000, infant: 0 })
    expect(q?.total).toBe(962000 * 2 + 559000)
    expect(q?.season).toEqual({ name: 'Lễ 30/4', percent: 30 })
  })

  it('mùa giảm 15% cũng làm tròn LÊN nghìn', () => {
    const q = computeQuote(bang, { adult: 1, child: 1, senior: 0, infant: 0 }, { seasons: mua, departDate: '2027-10-05' })
    // 740.000 × 0,85 = 629.000 (tròn sẵn) ; 430.000 × 0,85 = 365.500 → 366.000
    expect(q?.perPax).toEqual({ adult: 629000, child: 366000 })
    expect(q?.season).toEqual({ name: 'Thấp điểm', percent: -15 })
  })

  it('em bé miễn phí vẫn miễn phí ở mọi mùa', () => {
    const q = computeQuote(bang, { adult: 1, child: 0, senior: 0, infant: 2 }, { seasons: mua, departDate: '2027-07-01' })
    expect(q?.perPax.infant).toBe(0)
    expect(q?.lines.find(l => l.code === 'infant')?.subtotal).toBe(0)
  })

  it('không truyền mùa → y hệt hành vi cũ', () => {
    const a = computeQuote(bang, { adult: 2, child: 0, senior: 0, infant: 0 })
    const b = computeQuote(bang, { adult: 2, child: 0, senior: 0, infant: 0 }, {})
    expect(a?.total).toBe(1480000)
    expect(b?.total).toBe(1480000)
    expect(a?.season).toBeUndefined()
  })

  it('bảng giá theo bậc cũng áp được mùa', () => {
    const bac: PriceTable = { kind: 'tiers', tiers: [{ maxPax: 6, amount: 900000 }] }
    const q = computeQuote(bac, { adult: 3, child: 0, senior: 0, infant: 0 }, { seasons: mua, departDate: '2027-07-01' })
    // 900.000 × 1,2 = 1.080.000
    expect(q?.total).toBe(1080000 * 3)
    expect(q?.season?.name).toBe('Cao điểm hè')
  })
})
```

- [ ] **Bước 2: Chạy test cho thất bại**

Chạy: `npx vitest run test/booking/quote.test.ts`
Kỳ vọng: FAIL — `computeQuote` chưa nhận tham số thứ ba; `q.season` không tồn tại.

- [ ] **Bước 3: Sửa `quote.ts`**

Thêm import và kiểu ở đầu file:

```ts
import { pickSeason, type Season } from './season'

export type QuoteOptions = { seasons?: Season[]; departDate?: string }
export type Quote = {
  lines: QuoteLine[]
  total: number
  perPax: Partial<Record<PaxCode, number>>
  /** Mùa đã áp, để đơn ghi lại vì sao ra con số này (ADR-0030 §3). */
  season?: { name: string; percent: number }
}
```

Thêm hàm áp phần trăm ngay trên `computeQuote`:

```ts
/** Làm tròn LÊN nghìn sau khi áp phần trăm (ADR-0030 §3). */
function apDieuChinh(amount: number, percent: number): number {
  if (!percent) return amount
  return Math.ceil((amount * (100 + percent)) / 100 / 1000) * 1000
}
```

Sửa thân `computeQuote` — chỉ ba chỗ, phần còn lại giữ nguyên:

```ts
export function computeQuote(table: PriceTable, pax: PaxCounts, opts: QuoteOptions = {}): Quote | null {
  const n = totalPax(pax)
  if (n <= 0) return null

  const mua = opts.seasons && opts.departDate ? pickSeason(opts.seasons, opts.departDate) : null
  const pct = mua?.percent ?? 0
  const nhan = (x: number) => apDieuChinh(x, pct)
  const kem = (q: Omit<Quote, 'season'>): Quote => (mua ? { ...q, season: { name: mua.name, percent: mua.percent } } : q)

  if (table.kind === 'tiers') {
    const tier = [...table.tiers].sort((a, b) => a.maxPax - b.maxPax).find(t => t.maxPax >= n)
    if (!tier) return null
    const amount = nhan(tier.amount)
    return kem({
      lines: [{ code: 'adult', count: n, amount, subtotal: amount * n }],
      total: amount * n,
      perPax: { adult: amount },
    })
  }

  const lines: QuoteLine[] = []
  const perPax: Partial<Record<PaxCode, number>> = {}
  for (const code of PAX_ORDER) {
    const count = pax[code] || 0
    if (count <= 0) continue
    const goc = table.perPax[code]
    if (typeof goc !== 'number') return null
    const amount = nhan(goc)
    lines.push({ code, count, amount, subtotal: amount * count })
    perPax[code] = amount
  }
  return kem({ lines, total: lines.reduce((s, l) => s + l.subtotal, 0), perPax })
}
```

- [ ] **Bước 4: Chạy toàn bộ test**

Chạy: `npx vitest run`
Kỳ vọng: tất cả pass — 11 ca cũ của `quote.test.ts` vẫn đúng (không truyền `opts` thì
`pct = 0`, `apDieuChinh` trả nguyên giá).

- [ ] **Bước 5: Kiểm kiểu và commit**

```bash
npx astro check 2>&1 | tail -3
git add src/lib/booking/quote.ts test/booking/quote.test.ts
git commit -m "feat(gia): computeQuote ap mua vu, lam tron len nghin"
```

---

### Task 3: Loại tài liệu mùa trong Sanity Studio

**Files:**
- Create: `cms/schemas/bangGiaMuaVu.ts`
- Modify: `cms/schemas/index.ts`

**Interfaces:**
- Consumes: không.
- Produces: tài liệu Sanity `_type == 'bangGiaMuaVu'`, một bản duy nhất, có mảng `muaVu[]` với
  các trường `tenMua` (string), `tuNgay` (date), `denNgay` (date), `phanTram` (number),
  `apCho` (array of string — khoá giá), `truRa` (array of string). Task 4 truy vấn đúng các
  tên này.

- [ ] **Bước 1: Viết schema**

```ts
// cms/schemas/bangGiaMuaVu.ts
import { defineType, defineField, defineArrayMember } from 'sanity'
import { CalendarIcon } from '@sanity/icons'

// Bảng mùa vụ: KHÔNG chứa giá, chỉ chứa quy tắc điều chỉnh (ADR-0030 §3). Nguồn giá vẫn là
// Google Sheet → data/prices.yaml. Thứ tự các mục trong `muaVu` LÀ độ ưu tiên: mục trên thắng
// mục dưới. Kéo thả để sắp lại — không cần sửa mã.
export default defineType({
  name: 'bangGiaMuaVu',
  title: 'Giá theo mùa',
  type: 'document',
  icon: CalendarIcon,
  fields: [
    defineField({
      name: 'muaVu',
      title: 'Danh sách mùa — mục TRÊN thắng mục DƯỚI',
      description:
        'Khi một ngày rơi vào nhiều mùa, hệ dùng mùa đứng cao nhất trong danh sách rồi dừng. ' +
        'Kéo thả để đổi độ ưu tiên. Phần trăm dương là tăng giá, âm là giảm giá.',
      type: 'array',
      of: [
        defineArrayMember({
          type: 'object',
          name: 'mua',
          fields: [
            defineField({
              name: 'tenMua', title: 'Tên mùa', type: 'string',
              description: 'Hiện trong thư báo đơn để nhân viên biết vì sao ra con số đó.',
              validation: Rule => Rule.required().max(60),
            }),
            defineField({
              name: 'tuNgay', title: 'Từ ngày', type: 'date',
              options: { dateFormat: 'DD/MM/YYYY' },
              validation: Rule => Rule.required(),
            }),
            defineField({
              name: 'denNgay', title: 'Đến ngày', type: 'date',
              description: 'Tính cả ngày này. Ngày lễ đơn lẻ thì điền trùng với Từ ngày.',
              options: { dateFormat: 'DD/MM/YYYY' },
              validation: Rule => Rule.required(),
            }),
            defineField({
              name: 'phanTram', title: 'Điều chỉnh (%)', type: 'number',
              description: 'Ví dụ 30 là tăng 30%, -15 là giảm 15%. Giá sau điều chỉnh làm tròn lên nghìn.',
              validation: Rule => Rule.required().min(-90).max(200),
            }),
            defineField({
              name: 'apCho', title: 'Chỉ áp cho các khoá giá', type: 'array',
              of: [{ type: 'string' }],
              description: 'Bỏ trống = áp cho mọi tour. Điền khoá giá (cột "Khoá giá" trong bảng tính).',
            }),
            defineField({
              name: 'truRa', title: 'Trừ ra các khoá giá', type: 'array',
              of: [{ type: 'string' }],
              description: 'Các tour KHÔNG áp mùa này, kể cả khi ô trên bỏ trống.',
            }),
          ],
          preview: {
            select: { title: 'tenMua', tu: 'tuNgay', den: 'denNgay', pct: 'phanTram' },
            prepare({ title, tu, den, pct }) {
              const dau = typeof pct === 'number' && pct > 0 ? '+' : ''
              return { title: `${title ?? '(chưa đặt tên)'} · ${dau}${pct ?? 0}%`, subtitle: `${tu ?? '?'} → ${den ?? '?'}` }
            },
          },
        }),
      ],
    }),
  ],
  preview: { prepare: () => ({ title: 'Giá theo mùa' }) },
})
```

- [ ] **Bước 2: Đăng ký schema**

Trong `cms/schemas/index.ts`, thêm import cạnh các import khác và đưa vào danh sách schema
xuất ra (theo đúng khuôn file đó đang dùng — đọc kỹ phần cuối file trước khi sửa, vì nó có cơ
chế bật/tắt theo `enabledEntities`; `bangGiaMuaVu` **không** đi qua cơ chế đó, đăng ký thẳng
như `siteSettings`):

```ts
import bangGiaMuaVu from './bangGiaMuaVu'
```

- [ ] **Bước 3: Kiểm Studio dựng được**

Chạy: `cd cms && npx sanity build --no-minify 2>&1 | tail -5`
Kỳ vọng: build xong, không lỗi schema.

- [ ] **Bước 4: Commit**

```bash
git add cms/schemas/bangGiaMuaVu.ts cms/schemas/index.ts
git commit -m "feat(cms): loai tai lieu Gia theo mua — danh sach co thu tu uu tien"
```

- [ ] **Bước 5: Việc của chủ dự án (không tự làm)**

Deploy Studio (`cd cms && npx sanity deploy`) rồi tạo một tài liệu "Giá theo mùa" với ít nhất
hai mùa để Task 4 có dữ liệu thật mà kiểm. **Đây là ghi dữ liệu Sanity** — hook
`guard-data-mutation` chặn tác nhân tự làm; chủ dự án tạo trong Studio.

---

### Task 4: Đọc mùa lúc dựng trang và nướng vào form

**Files:**
- Create: `src/lib/queries/seasons.ts`
- Modify: `src/components/TourDetail.astro`
- Modify: `src/components/BookingForm.astro` (chỉ thêm một thuộc tính `data-*`)

**Interfaces:**
- Consumes: `Season` (Task 1); tài liệu `bangGiaMuaVu` (Task 3).
- Produces: `fetchSeasons(): Promise<SeasonRule[]>` với
  `type SeasonRule = Season & { apCho: string[]; truRa: string[] }`;
  `seasonsForKey(rules: SeasonRule[], bookingKey: string): Season[]` — lọc theo tour, **giữ
  nguyên thứ tự**. `BookingForm` nhận prop `seasons: Season[]` và nướng vào
  `data-seasons` dạng JSON.

- [ ] **Bước 1: Viết test cho hàm lọc**

```ts
// test/booking/season.test.ts — thêm vào cuối
import { seasonsForKey, type SeasonRule } from '../../src/lib/queries/seasons'

describe('seasonsForKey', () => {
  const R: SeasonRule[] = [
    { name: 'Lễ', from: '2027-04-30', to: '2027-05-01', percent: 30, apCho: [], truRa: ['du-thuyen-vega-day-tour'] },
    { name: 'Hè', from: '2027-06-01', to: '2027-08-31', percent: 20, apCho: [], truRa: [] },
    { name: 'Riêng Hòn Tằm', from: '2027-09-01', to: '2027-09-30', percent: 10, apCho: ['tour-hon-tam-tron-goi'], truRa: [] },
  ]
  it('apCho rỗng = áp mọi tour', () => {
    expect(seasonsForKey(R, 'tour-3-dao-nha-trang-hon-mun-hon-tam').map(s => s.name)).toEqual(['Lễ', 'Hè'])
  })
  it('truRa loại đúng tour đó', () => {
    expect(seasonsForKey(R, 'du-thuyen-vega-day-tour').map(s => s.name)).toEqual(['Hè'])
  })
  it('apCho có giá trị = chỉ tour trong danh sách', () => {
    expect(seasonsForKey(R, 'tour-hon-tam-tron-goi').map(s => s.name)).toEqual(['Lễ', 'Hè', 'Riêng Hòn Tằm'])
  })
  it('giữ nguyên thứ tự ưu tiên sau khi lọc', () => {
    const out = seasonsForKey(R, 'tour-hon-tam-tron-goi')
    expect(out[0].name).toBe('Lễ')
  })
})
```

- [ ] **Bước 2: Chạy cho thất bại**

Chạy: `npx vitest run test/booking/season.test.ts`
Kỳ vọng: FAIL — chưa có module `queries/seasons`.

- [ ] **Bước 3: Viết truy vấn và hàm lọc**

```ts
// src/lib/queries/seasons.ts
// Đọc bảng mùa lúc DỰNG TRANG. File này thuộc tầng dữ liệu build, KHÔNG được import từ
// src/lib/booking/* (BK1) — TourDetail.astro là nơi ghép hai thứ lại.
import { sanityClient } from '../sanity'
import type { Season } from '../booking/season'

export type SeasonRule = Season & { apCho: string[]; truRa: string[] }

const QUERY = `*[_type == "bangGiaMuaVu"][0].muaVu[]{
  "name": tenMua, "from": tuNgay, "to": denNgay, "percent": phanTram,
  "apCho": coalesce(apCho, []), "truRa": coalesce(truRa, [])
}`

export async function fetchSeasons(): Promise<SeasonRule[]> {
  const rows = await sanityClient.fetch<SeasonRule[] | null>(QUERY)
  if (!rows) return []
  // Bỏ dòng thiếu trường bắt buộc thay vì để form nhận dữ liệu rác. Validator (Task 7) mới là
  // nơi báo đỏ; ở đây chỉ cần không làm hỏng trang.
  return rows.filter(r => r && r.name && r.from && r.to && typeof r.percent === 'number')
}

/** Lọc mùa áp được cho một tour, GIỮ NGUYÊN thứ tự ưu tiên. */
export function seasonsForKey(rules: SeasonRule[], bookingKey: string): Season[] {
  return rules
    .filter(r => !r.truRa.includes(bookingKey))
    .filter(r => r.apCho.length === 0 || r.apCho.includes(bookingKey))
    .map(({ name, from, to, percent }) => ({ name, from, to, percent }))
}
```

Kiểm tên export của client Sanity trong `src/lib/sanity.ts` trước khi viết dòng import — nếu
tên khác `sanityClient` thì dùng đúng tên đang có, không đổi file đó.

- [ ] **Bước 4: Chạy test cho pass**

Chạy: `npx vitest run test/booking/season.test.ts`
Kỳ vọng: PASS 13/13.

- [ ] **Bước 5: Ghép vào TourDetail và BookingForm**

Trong `src/components/TourDetail.astro`, cạnh chỗ dựng `priceTable` (khoảng dòng 42):

```astro
const seasonRules = await fetchSeasons()
const seasons = bookingKey ? seasonsForKey(seasonRules, bookingKey) : []
```

Truyền xuống form (chỗ render `<BookingForm ... />`, khoảng dòng 201):

```astro
seasons={seasons}
```

Trong `src/components/BookingForm.astro`, thêm vào `Props` và nướng vào thẻ `<form>`:

```astro
seasons?: Season[]
```

```astro
data-seasons={JSON.stringify(seasons ?? [])}
```

- [ ] **Bước 6: Dựng thử và kiểm dấu vết**

```bash
npx astro build 2>&1 | grep -E "error|Complete" | tail -2
grep -o 'data-seasons="[^"]*"' dist/tour/ve-hon-tam-tron-goi-tour-hon-tam-1-ngay/index.html | head -c 300
```

Kỳ vọng: build xong; thuộc tính `data-seasons` có mặt và chứa đúng các mùa chủ dự án đã tạo ở
Task 3 bước 5 (nếu chưa tạo, giá trị là `[]` — vẫn hợp lệ).

- [ ] **Bước 7: Commit**

```bash
git add src/lib/queries/seasons.ts test/booking/season.test.ts src/components/TourDetail.astro src/components/BookingForm.astro
git commit -m "feat(gia): doc bang mua luc dung trang, nuong vao form theo tung tour"
```

---

### Task 5: Trình duyệt tính lại khi khách đổi ngày

**Files:**
- Modify: `src/components/BookingForm.astro` (phần `<script>`)

**Interfaces:**
- Consumes: `data-seasons` (Task 4); `computeQuote(table, pax, opts)` (Task 2).
- Produces: không có API mới. Hành vi: đổi ngày khởi hành thì tạm tính đổi theo, và dòng tên
  mùa hiện ngay dưới bảng tính tiền.

- [ ] **Bước 1: Đọc mùa từ thuộc tính và truyền vào phép tính**

Trong `init(form)`, cạnh chỗ đọc `table`:

```ts
const seasons = JSON.parse(form.dataset.seasons || '[]') as Season[]
```

Trong `update()`, đổi lời gọi `computeQuote`:

```ts
quote = computeQuote(table, pax, { seasons, departDate: dateEl.value })
```

Thêm `dateEl.addEventListener('change', update)` nếu chưa có — đổi ngày phải kích tính lại.

- [ ] **Bước 2: Hiện tên mùa cho khách thấy**

Thêm một dòng trong markup, ngay dưới `.bf__quote`:

```astro
<p class="bf__season" data-season hidden></p>
```

Trong `update()`, sau khi đã có `quote`:

```ts
const seasonEl = q<HTMLElement>('[data-season]')!
if (quote?.season) {
  const dau = quote.season.percent > 0 ? '+' : ''
  seasonEl.textContent = `${quote.season.name} · ${dau}${quote.season.percent}%`
  seasonEl.hidden = false
} else {
  seasonEl.textContent = ''
  seasonEl.hidden = true
}
```

CSS, dùng token sẵn có, và **phải** thêm `.bf__season[hidden]` vào danh sách `display: none`
đang có ở cuối khối style — nếu quên, khối này sẽ hiện thường trực đúng như bẫy đã ghi ở
`DR-102`:

```css
.bf__season { font-size: var(--fs-sm); color: var(--c-accent-strong); margin: 0; }
```

- [ ] **Bước 3: Dựng và kiểm trên trình duyệt**

```bash
npx astro build 2>&1 | grep -E "error|Complete" | tail -2
```

Rồi mở bản dựng cục bộ (`cd dist && python3 -m http.server 4321`) và kiểm bằng
`javascript_tool` — **không chụp màn hình rồi cuộn**, thao tác đó đã làm treo Chrome nhiều lần
ở dự án này; `:root` có `scroll-behavior: smooth` nên dùng `scrollTo({behavior:'instant'})`:

1. Chọn một ngày **ngoài** mọi mùa → dòng `[data-season]` phải `hidden`, tạm tính bằng giá gốc.
2. Chọn một ngày **trong** một mùa tăng → tạm tính tăng đúng phần trăm sau khi làm tròn lên
   nghìn, dòng mùa hiện tên và mức.
3. Đổi sang ngày trong mùa khác → số đổi theo ngay, không tải lại trang.

- [ ] **Bước 4: Commit**

```bash
git add src/components/BookingForm.astro
git commit -m "feat(gia): form tinh lai tam tinh theo mua khi khach doi ngay"
```

---

### Task 6: Mùa đã áp đi vào đơn, thư và tin Zalo

**Files:**
- Modify: `src/lib/booking/schema.ts`
- Modify: `src/lib/booking/notify/format.ts`
- Test: `test/booking/schema.test.ts`, `test/booking/notify.test.ts`

**Interfaces:**
- Consumes: `Quote.season` (Task 2).
- Produces: payload đơn nhận thêm `quoted.season?: { name: string; percent: number }`; hàm
  dựng nội dung thư/tin thêm một dòng `Mùa áp dụng: <tên> (+N%)` khi có.

- [ ] **Bước 1: Viết test thất bại**

```ts
// test/booking/schema.test.ts — thêm
it('nhận quoted.season và giữ nguyên khi hợp lệ', () => {
  const input = parseBookingPayload({
    tourSlug: 'x', tourTitle: 'X', bookingRef: 'x', departDate: '2027-04-30',
    pax: { adult: 1 }, quoted: { perPax: { adult: 962000 }, total: 962000, quotedAt: '2027-01-01T00:00:00.000Z', season: { name: 'Lễ 30/4', percent: 30 } },
    hoTen: 'A', soDienThoai: '0905123456',
  })
  expect(input.quoted.season).toEqual({ name: 'Lễ 30/4', percent: 30 })
})
it('season rác thì bỏ, không ném', () => {
  const input = parseBookingPayload({
    tourSlug: 'x', tourTitle: 'X', bookingRef: 'x', departDate: '2027-04-30',
    pax: { adult: 1 }, quoted: { perPax: { adult: 1 }, total: 1, quotedAt: '2027-01-01T00:00:00.000Z', season: 'không phải object' },
    hoTen: 'A', soDienThoai: '0905123456',
  })
  expect(input.quoted.season).toBeUndefined()
})
```

```ts
// test/booking/notify.test.ts — thêm
it('có mùa thì thư ghi thêm một dòng', () => {
  const text = formatText({ ...donMau, quotedJson: JSON.stringify({ perPax: { adult: 962000 }, total: 962000, season: { name: 'Lễ 30/4', percent: 30 } }) })
  expect(text).toContain('Mùa áp dụng: Lễ 30/4 (+30%)')
})
it('không mùa thì không có dòng đó', () => {
  const text = formatText(donMau)
  expect(text).not.toContain('Mùa áp dụng')
})
```

Đọc `test/booking/notify.test.ts` để lấy đúng tên biến đơn mẫu đang dùng và hình dạng tham số
của `formatText` trước khi viết — không đoán tên.

- [ ] **Bước 2: Chạy cho thất bại**

Chạy: `npx vitest run test/booking/schema.test.ts test/booking/notify.test.ts`
Kỳ vọng: FAIL ở cả bốn ca mới.

- [ ] **Bước 3: Sửa `schema.ts`**

Trong hàm dựng `quoted` của `parseBookingPayload`, thêm:

```ts
// Mùa chỉ để GHI LẠI vì sao ra con số này — server không tin và không tính lại theo nó
// (BK1: server không đọc giá). Nhận vào thì làm sạch, sai hình dạng thì bỏ.
const s = (r.quoted as Record<string, unknown> | undefined)?.season
const season = s && typeof s === 'object' && typeof (s as any).name === 'string' && typeof (s as any).percent === 'number'
  ? { name: clean(String((s as any).name)).slice(0, 60), percent: (s as any).percent }
  : undefined
```

Đưa `season` vào đối tượng `quoted` trả về, và thêm vào kiểu `BookingInput`/`BookingValid`
tương ứng. `quoted` đã được lưu nguyên vào cột `quoted_json` nên **không cần migration D1**.

- [ ] **Bước 4: Sửa `notify/format.ts`**

Trong `formatText`, ngay sau dòng tạm tính:

```ts
const q = JSON.parse(b.quotedJson || '{}') as { season?: { name: string; percent: number } }
if (q.season) {
  const dau = q.season.percent > 0 ? '+' : ''
  lines.push(`Mùa áp dụng: ${q.season.name} (${dau}${q.season.percent}%)`)
}
```

Đọc file để dùng đúng tên trường chứa JSON tạm tính của đơn — không đoán.

- [ ] **Bước 5: Chạy toàn bộ test**

Chạy: `npx vitest run`
Kỳ vọng: tất cả pass.

- [ ] **Bước 6: Commit**

```bash
npx astro check 2>&1 | tail -3
git add src/lib/booking/schema.ts src/lib/booking/notify/format.ts test/booking/schema.test.ts test/booking/notify.test.ts
git commit -m "feat(gia): don va thu bao don ghi lai mua da ap"
```

---

### Task 7: Validator canh dữ liệu mùa

**Files:**
- Create: `scripts/validators/mua-vu.ts`
- Modify: `scripts/validators/index.ts` (hoặc file tập hợp validator — đọc để biết khuôn đăng ký)
- Test: `scripts/validators/__tests__/mua-vu.test.ts`

**Interfaces:**
- Consumes: `SeasonRule` (Task 4).
- Produces: `validateMuaVu(rules: SeasonRule[], priceKeys: Set<string>): { errors: string[] }`.

- [ ] **Bước 1: Viết test thất bại**

```ts
// scripts/validators/__tests__/mua-vu.test.ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import { validateMuaVu } from '../mua-vu'

const KEYS = new Set(['tour-hon-tam-tron-goi', 'du-thuyen-vega-day-tour'])

test('dữ liệu đúng thì không lỗi', () => {
  const r = [{ name: 'Hè', from: '2027-06-01', to: '2027-08-31', percent: 20, apCho: [], truRa: [] }]
  assert.equal(validateMuaVu(r as any, KEYS).errors.length, 0)
})
test('ngày kết thúc trước ngày bắt đầu là lỗi', () => {
  const r = [{ name: 'Sai', from: '2027-08-31', to: '2027-06-01', percent: 20, apCho: [], truRa: [] }]
  assert.match(validateMuaVu(r as any, KEYS).errors[0], /Đến ngày/)
})
test('phần trăm ngoài khoảng là lỗi', () => {
  const r = [{ name: 'Quá', from: '2027-06-01', to: '2027-08-31', percent: 500, apCho: [], truRa: [] }]
  assert.match(validateMuaVu(r as any, KEYS).errors[0], /phần trăm/i)
})
test('khoá giá không tồn tại là lỗi', () => {
  const r = [{ name: 'Hè', from: '2027-06-01', to: '2027-08-31', percent: 20, apCho: ['khong-co-that'], truRa: [] }]
  assert.match(validateMuaVu(r as any, KEYS).errors[0], /khong-co-that/)
})
test('hai mùa phủ nhau KHÔNG phải lỗi — thứ tự quyết định', () => {
  const r = [
    { name: 'Lễ', from: '2027-04-30', to: '2027-05-01', percent: 30, apCho: [], truRa: [] },
    { name: 'Hè', from: '2027-04-01', to: '2027-08-31', percent: 20, apCho: [], truRa: [] },
  ]
  assert.equal(validateMuaVu(r as any, KEYS).errors.length, 0)
})
```

- [ ] **Bước 2: Chạy cho thất bại**

Chạy: `cd scripts && npx tsx --test validators/__tests__/mua-vu.test.ts`
Kỳ vọng: FAIL — chưa có module.

- [ ] **Bước 3: Viết validator**

```ts
// scripts/validators/mua-vu.ts
import type { SeasonRule } from '../../src/lib/queries/seasons'

const ISO = /^\d{4}-\d{2}-\d{2}$/

/**
 * Ba luật. KHÔNG có luật nào về chồng lấn: hai mùa phủ nhau là hợp lệ, thứ tự trong Studio
 * quyết định cái nào thắng (ADR-0030 §3).
 */
export function validateMuaVu(rules: SeasonRule[], priceKeys: Set<string>): { errors: string[] } {
  const errors: string[] = []
  rules.forEach((r, i) => {
    const nhan = `mùa #${i + 1} "${r.name || '(chưa đặt tên)'}"`
    if (!ISO.test(r.from) || !ISO.test(r.to)) {
      errors.push(`${nhan}: ngày phải dạng YYYY-MM-DD`)
    } else if (r.from > r.to) {
      errors.push(`${nhan}: "Đến ngày" (${r.to}) đứng trước "Từ ngày" (${r.from})`)
    }
    if (typeof r.percent !== 'number' || r.percent < -90 || r.percent > 200) {
      errors.push(`${nhan}: phần trăm ${r.percent} ngoài khoảng cho phép (-90…200)`)
    }
    for (const k of [...(r.apCho ?? []), ...(r.truRa ?? [])]) {
      if (!priceKeys.has(k)) errors.push(`${nhan}: khoá giá "${k}" không có trong data/prices.yaml`)
    }
  })
  return { errors }
}
```

- [ ] **Bước 4: Chạy test cho pass**

Chạy: `cd scripts && npx tsx --test validators/__tests__/mua-vu.test.ts`
Kỳ vọng: 5/5 pass.

- [ ] **Bước 5: Đăng ký vào bộ validator**

Đọc `scripts/validators/` để biết bộ kiểm hiện đăng ký thế nào (xem cách `py1-py8` được gọi
trong `npm --prefix scripts run validate`), rồi thêm `validateMuaVu` theo đúng khuôn đó: đọc
mùa từ Sanity, đọc khoá giá từ `data/prices.yaml`, báo lỗi làm build dừng.

- [ ] **Bước 6: Chạy bộ kiểm đầy đủ và commit**

```bash
npm --prefix scripts run validate 2>&1 | tail -8
git add scripts/validators/mua-vu.ts scripts/validators/__tests__/mua-vu.test.ts scripts/validators/index.ts
git commit -m "feat(gia): validator canh du lieu mua vu"
```

---

## Sau khi xong bảy task

Việc của chủ dự án, không tự làm: tạo bảng mùa thật trong Studio, bấm Publish, chờ dựng lại,
rồi mở một trang tour kiểm tạm tính đổi đúng theo ngày khởi hành. Sau đó gửi một đơn thử và
xoá khỏi D1 như quy trình nghiệm thu đã dùng.

Bước tiếp theo của `ADR-0030` là lớp bề mặt (token cho thư từ) — kế hoạch riêng, không gộp vào
đây.
